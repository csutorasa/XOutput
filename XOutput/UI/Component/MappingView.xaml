<UserControl x:Class="XOutput.UI.Component.MappingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XOutput.UI.Component"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Margin="2" Content="{Binding XInputType}"/>
        <ComboBox Grid.Column="1" Margin="2" ItemsSource="{Binding Inputs}" SelectedItem="{Binding SelectedInput, Mode=TwoWay}"/>
        <DockPanel Grid.Column="2" Margin="2">
            <Label>Min:</Label>
            <local:NumericTextBox Height="20" Minimum="0" Maximum="100" Value="{Binding Min, Mode=TwoWay}"/>
        </DockPanel>
        <DockPanel Grid.Column="3" Margin="2">
            <Label>Max:</Label>
            <local:NumericTextBox Height="20" Minimum="0" Maximum="100" Value="{Binding Max, Mode=TwoWay}"/>
        </DockPanel>
    </Grid>
</UserControl>
