<UserControl x:Class="XOutput.UI.Component.ControllerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XOutput.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="70"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <TextBlock Margin="10" Text="{Binding DisplayName}" TextWrapping="NoWrap" HorizontalAlignment="Stretch"/>
        </Grid>
        <Button Grid.Column="1" FontSize="14" Margin="5" Click="Open_Click">Open</Button>
        <Button Grid.Column="2" FontSize="14" Margin="5" Click="Button_Click" Content="{Binding ButtonText}"/>
    </Grid>
</UserControl>
